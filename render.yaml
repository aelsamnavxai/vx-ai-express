services:
  - type: web
    name: vx-ai-express
    env: node
    buildCommand: |
      cd /opt/render/project/src
      npm install
      npm run build
    startCommand: |
      cd /opt/render/project/src
      npm start